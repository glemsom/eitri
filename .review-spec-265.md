# Spec review sub-agent task

Repo: /home/glemsom/Documents/git/eitri (branch issue-265-thinking-suppression-capability; main is the fixed point).

## Diff to review
Run: `git diff main...HEAD` (three-dot against merge-base). Commit list: `git log main..HEAD --oneline` (single commit: 19dd9b6).

## Spec
GitHub issue #265, fetched via `gh issue view 265`. Acceptance criteria:
- [ ] A provider capability exists describing whether thinking suppression (thinking-off) is supported, in the same style as the existing generation controls.
- [ ] opencode-go / copilot advertise their true support once the wire is fixed; custom-openai advertises suppression support.
- [ ] When the user has thinking off and the selected provider does NOT support suppression, the UI warns (settings panel or status) that reasoning cannot be disabled on this provider.
- [ ] A test asserts each provider's advertised capability matches its wire behavior.

## Brief
Report: (a) requirements the spec asked for that are missing or partial; (b) behaviour in the diff that wasn't asked for (scope creep); (c) requirements that look implemented but where the implementation looks wrong. Quote the spec line for each finding. Under 400 words.