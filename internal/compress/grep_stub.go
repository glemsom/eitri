package compress

// compressGrep is a stub — will be implemented in a follow-up ticket (#938).
// Returning nil means "no compression applied", which passes through unchanged.
func compressGrep(output string) *string {
	return nil
}
